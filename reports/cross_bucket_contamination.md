# Cross-bucket contamination check

MinHash/LSH over 24646 units (est. Jaccard ≥0.7). A *cross-bucket* near-dup pair would corrupt the offense/dual/defense labels.

**Found 0 cross-bucket near-duplicate pairs.**

✅ No cross-bucket contamination — the buckets are cleanly separated by provenance (as expected from disjoint sources).
